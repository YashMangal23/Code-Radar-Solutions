#include <stdio.h>

int main() {
    char ch;
    
    
    scanf("%c", &ch);  // Read a single character

    printf("ASCII Value: %d\n",(int)ch);  // Print the ASCII value

    return 0;
}
