#include <stdio.h>

int main() {
    char ch;
    
    
    scanf("%c", &ch);  // Read a single character

    printf("ASCII Value: '%c'%d\n", ch, (int)ch);  // Print the ASCII value

    return 0;
}
